[build]
  command = "npm install"
  functions = "netlify/functions"
  publish = "public"

# Redirect /api/* requests to Netlify Functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
